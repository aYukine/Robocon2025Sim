scenario1 = [ 
            # robot 1
                [("Move", (700, 200)),
                ("Dribble", ()),
                ("Move", (900, 300)),
                ("Shoot", ()) ],
            # robot 2
                [("Move", (700, 500)),
                ("Wait", 9),
                ("Move", (1100, 500)),],
            # robot 3
                [],
            # robot 4
                []
]

scenario2 = [ 
            # robot 1
                [("Move", (700, 200)),
                ("Dribble", ()),
                ("Move", (900, 300)),
                ("Shoot", ()) ],
            # robot 2
                [("Move", (700, 500)),
                ("Wait", 9),
                ("Move", (1100, 500)),],
            # robot 3
                [],
            # robot 4
                []
]



current_trajectory = scenario1